# AT32F402RCT7-7 项目模板

## 项目结构

```
├── fireware/           # AT32F402/405驱动库文件
│   ├── inc/           # 驱动头文件
│   └── src/           # 驱动源文件
├── libraries/         # 库文件
│   └── cmsis/         # CMSIS标准库
├── bsp/               # 板级支持包
├── user/              # 用户应用代码
├── cmsis/             # CMSIS核心文件
└── mdkv5/             # Keil MDK-ARM项目文件
```

## 重要说明

### 路径配置
- 驱动文件已从 `libraries/drivers/` 迁移至 `fireware/` 目录
- 编译前请检查项目包含路径配置是否正确
- 注意警告配置，确保编译环境正确设置

### 编译配置
请参考编译配置图片进行项目设置

## 更新日志

### 2026-02-03
- 重构项目结构，将AT32F402/405驱动文件从 `libraries/drivers/` 移至 `fireware/` 目录
- 更新项目配置文件以适应新的目录结构
- 优化项目组织结构

### 2026-02-01
- 初始化项目模板
- 添加项目配置文件和文档